"use client";

import React from "react";
import { Button } from "./ui/button";

const FilterButton = () => {
  return (
    <Button variant="ghost" className="text-foreground font-exo">
      Filter
    </Button>
  );
};

export default FilterButton;
